extends: default

rules:
  # 80 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 180
    level: warning
  truthy: 
    allowed-values:
      - "yes"
      - "no"
      - "true"
      - "false"